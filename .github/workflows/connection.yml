name: Connection

on:
  schedule:
    - cron: '0 12 14,15,16 * *'

  workflow_dispatch:


jobs:
  ConnectionCheck:
    name: ConnectionCheck
    runs-on: self-hosted-mh

    steps:
       run: echo "hello"
